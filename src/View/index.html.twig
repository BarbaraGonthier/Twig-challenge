{% extends "base.html.twig" %}

{% block title %} Index {% endblock %}

{% foreach ($products ad $product) %}
<div class="row">
  <div class="col-sm-4">
    <div class="card">
      <div class="card-body">
        <h2> class="card-title">{{ product }}</h2>
      </div>
    </div>
  </div>
{%  endforeach %}
